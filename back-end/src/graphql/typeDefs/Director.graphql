type Director {
    name: String
    birthday: String
    country: String
}

input DirectorsInput {
    name: String!
    birthday: String!
    country: String! 
    actor: ID
}

type Mutation {
    createDirectors(input: DirectorsInput): Director!
}